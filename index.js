const providerName = (phonenumber) => {
    //test the arguments
    //return the provider name
}

const getCountryProviders = (country) => {
    //get arguments from all the folders
    //return the array of providers
}

export { providerName, getCountryProviders }